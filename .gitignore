# ─── SECRETS — never commit ───────────────────────────────
.env
apikey.txt
gmail_sender.txt
gmail_app_password.txt

# ─── PERSONAL NOTES (not for the repo) ────────────────────
BUILDINGS.md
# NOTE: config.local.json IS committed in this personal/private tracker repo
# (filters + recipient only — no secrets) so filters can be edited without code
# changes. In the shareable digest project it stays gitignored.

# ─── LOCAL STATE & GENERATED OUTPUT ───────────────────────
snapshot.json
rentcast_usage.json
digest.html
digest_demo.html
test_raw_response.json
_np_sample.html
_*.html

# ─── PYTHON ───────────────────────────────────────────────
__pycache__/
*.pyc

# ─── EXPLICITLY KEEP THESE TEMPLATES (must ship) ──────────
!config.example.json
!.env.example
